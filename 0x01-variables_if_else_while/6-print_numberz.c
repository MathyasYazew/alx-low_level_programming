#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int nody;

	for (nody = '0' ; nody <= '9' ; nody++)
	{
	putchar(nody);
	}
	putchar('\n');

return (0);
}
